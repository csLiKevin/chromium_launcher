{
  "name": "chromium_launcher",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "check": "tsc",
    "compile_exe": "bun build index.ts --compile --target=bun-windows-x64 --outfile dist/chromium_launcher.exe",
    "start": "bun run index.ts"
  }
}
